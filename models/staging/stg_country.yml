version: 2

models:
  - name: stg_country
    description: "Staging table for country data"
    columns:
      - name: id
        description: "ID of the country"
        tests:
          - unique
          - not_null
      - name: name
        description: "Name of the country"
        tests:
          - unique
          - not_null